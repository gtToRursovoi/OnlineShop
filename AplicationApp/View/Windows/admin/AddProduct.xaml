<Window x:Class="AplicationApp.View.Windows.admin.AddProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AplicationApp.View.Windows.admin"
        mc:Ignorable="d"
        Title="AddProduct" Height="550" Width="500"
        Background="Pink">
    <Grid>
        <Image Source="/Assets/Image/Back2.jpg" Stretch="Fill" Panel.ZIndex="0">
            <Image.Effect>
                <BlurEffect Radius="10"/>
            </Image.Effect>
        </Image>
        <Rectangle Fill="#66000000" Panel.ZIndex="1"/>
        <StackPanel Margin="20" Panel.ZIndex="2">
            <Label Content="Имя" Foreground="White"/>
            <TextBox Text="{Binding Name}" Margin="0,0,0,10"/>

            <Label Content="Описание" Foreground="White"/>
            <TextBox Text="{Binding Description}" Margin="0,0,0,10"/>

            <Label Content="Цена" Foreground="White"/>
            <TextBox Text="{Binding Price}" Margin="0,0,0,10"/>

            <Label Content="Количество" Foreground="White"/>
            <TextBox Text="{Binding Stock}" Margin="0,0,0,10"/>

            <Button Content="Изображение" Command="{Binding SelectImageCommand}" Background="Violet" Margin="0,10,0,0"/>
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Background="Violet" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
