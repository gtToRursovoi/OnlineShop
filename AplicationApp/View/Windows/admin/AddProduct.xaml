<Window x:Class="AplicationApp.View.Windows.admin.AddProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AplicationApp.View.Windows.admin"
        mc:Ignorable="d"
        Title="AddProduct" Height="550" Width="500">
    <Grid>
        <StackPanel>
            <Label Content="Имя"/>
            <TextBox Text="{Binding Name}"/>
            <Label Content="Описание"/>
            <TextBox Text="{Binding Description}"/>
            <Label Content="Цена"/>
            <TextBox Text="{Binding Price}"/>
            <Label Content="Количество"/>
            <TextBox Text="{Binding Stock}"/>
            <Button Content="Изображение" Command="{Binding SelectImageCommand}" Background="Gray" Margin="10"/>
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Background="Gray" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
