<UserControl x:Class="AplicationApp.View.Pagess.User.basketPageUser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AplicationApp.View.Pagess.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Корзина" FontSize="24" FontWeight="Bold" Margin="10" Grid.Row="0"/>

        <ListView ItemsSource="{Binding CartItems}" Margin="10" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="Gray" Padding="10" Margin="5">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{Binding Product.ImageUrl}" Width="80" Height="80" Margin="10"/>
                            <StackPanel Width="200">
                                <TextBlock Text="{Binding Product.Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Product.Price, StringFormat={}{0:C}}"/>
                                <TextBlock Text="{Binding Product.Description}" FontSize="12" TextWrapping="Wrap"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="10">
                                <Button Content="-" 
                                        Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"
                                        Width="30"/>
                                <TextBlock Text="{Binding Quantity}" VerticalAlignment="Center" Margin="10"/>
                                <Button Content="+" 
                                        Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}"
                                        Width="30"/>
                            </StackPanel>

                            <Button Content="Удалить"
                                    Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="10"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Итоговая сумма и кнопка "Заказать" -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10" Grid.Row="2" >
            <TextBlock Text="{Binding TotalPrice, StringFormat=Итого: {0:C}}" 
                       FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Заказать" Width="120" Height="35" Command="{Binding PlaceOrderCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
