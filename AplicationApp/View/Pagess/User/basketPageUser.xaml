<UserControl x:Class="AplicationApp.View.Pagess.User.basketPageUser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AplicationApp.View.Pagess.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Корзина" FontSize="30" FontWeight="Bold" Margin="20,20,20,10" Foreground="#333" Grid.Row="0"/>

        <!-- Список товаров -->
        <ListView ItemsSource="{Binding CartItems}" Margin="20" Grid.Row="1" Background="Transparent" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,10" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Картинка -->
                            <Image Source="{Binding Product.ImageUrl}" Width="100" Height="100" Stretch="UniformToFill" Margin="0,0,15,0"/>

                            <!-- Информация -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Product.Name}" FontSize="16" FontWeight="Bold" Foreground="#222"/>
                                <TextBlock Text="{Binding Product.Price, StringFormat=Цена: {0:C}}" FontSize="14" Foreground="#444" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Product.Description}" FontSize="12" Foreground="#666" TextWrapping="Wrap" MaxWidth="300"/>
                            </StackPanel>

                            <!-- Количество -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0">
                                <Button Content="−"
                                        Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Width="35" Height="35" FontSize="16"/>
                                <TextBlock Text="{Binding Quantity}" VerticalAlignment="Center" Width="40" FontSize="16" TextAlignment="Center"/>
                                <Button Content="+"
                                        Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Width="35" Height="35" FontSize="16"/>
                            </StackPanel>

                            <!-- Удалить -->
                            <Button Grid.Column="3"
                                    Content="Удалить"
                                    Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Height="35" Width="100" Margin="20,0,0,0" VerticalAlignment="Center" Background="#E57373" Foreground="White"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Подвал: итого и кнопка -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20" Grid.Row="2" VerticalAlignment="Center">
            <TextBlock Text="{Binding TotalPrice, StringFormat=Итого: {0:C}}"
                       FontSize="20" FontWeight="Bold" Foreground="#222" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Оформить заказ"
                    Width="160" Height="40"
                    Command="{Binding PlaceOrderCommand}"
                    Background="#4CAF50" Foreground="White" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</UserControl>
